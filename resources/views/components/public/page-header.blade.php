<section class="page-header">
    <div class="container">
        <p class="eyebrow mb-2">MediCare Hospital</p>
        <h1 class="display-5 fw-bold mb-2">{{ $title }}</h1>
        @if ($subtitle)
            <p class="lead text-secondary mb-0">{{ $subtitle }}</p>
        @endif
    </div>
</section>
