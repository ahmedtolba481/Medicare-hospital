@extends('layouts.public')

@section('content')
    <x-public.page-header :title="$doctor->user->name" :subtitle="$doctor->specialization.' · '.$doctor->department->name" />
    <section class="section-padding"><div class="container"><div class="row g-5"><div class="col-lg-4"><div class="doctor-avatar doctor-avatar-lg">{{ \Illuminate\Support\Str::substr($doctor->user->name, 0, 1) }}</div></div><div class="col-lg-8"><p class="eyebrow mb-2">About {{ $doctor->user->name }}</p><h2 class="h3 mb-3">Personalized expertise for your health.</h2><p class="muted-copy">{{ $doctor->bio }}</p><div class="row g-3 mt-2"><div class="col-md-6"><div class="border rounded-3 p-3 h-100"><span class="text-secondary small d-block mb-1">Experience</span><strong>{{ $doctor->experience }} years in practice</strong></div></div><div class="col-md-6"><div class="border rounded-3 p-3 h-100"><span class="text-secondary small d-block mb-1">Education</span><strong>{{ $doctor->education }}</strong></div></div></div><div class="mt-4"><h3 class="h5">Consultation hours</h3><div class="d-flex flex-wrap gap-2">@forelse ($doctor->schedules as $schedule)<span class="badge text-bg-light border text-dark p-2">{{ $schedule->day }} · {{ \Illuminate\Support\Carbon::parse($schedule->start_time)->format('g:i A') }}–{{ \Illuminate\Support\Carbon::parse($schedule->end_time)->format('g:i A') }}</span>@empty<p class="text-secondary">Please contact MediCare for availability.</p>@endforelse</div></div><a class="btn btn-primary mt-4" href="{{ route('contact') }}">Contact MediCare</a></div></div></div></section>
@endsection
